# Supabase Edge Function (optional — the app works without these using a fallback)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# The Edge Function itself needs this (set in Supabase dashboard):
# OPENAI_API_KEY=sk-...
